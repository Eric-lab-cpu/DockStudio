# DockStudio 用户手册(© 2026 Eric Studio)

> 对应版本:**v2.0.0(桌面级可发表虚拟筛选工作台)** · © 2026 Eric Studio

本手册面向不熟悉命令行的用户。启动方式:安装版在开始菜单/桌面双击 **DockStudio**;
绿色免安装版解压后双击 `DockStudio.bat`;源码版在已安装依赖的环境中运行
`python -m dockstudio`。若需脚本/服务器自动化,可用
`python -m dockstudio run <工程文件(config.json 或 .dsproj)> [--out 目录]`
(见 README“headless CLI”一节)。

## 界面总览(v2.0 现代界面)

应用采用**左侧导航 + 顶部操作栏**的现代布局(基于 ttkbootstrap 主题;若未安装该
主题库会自动回退为标准外观):

- 左侧 **流程向导**:① 输入 → ② 结合位点 → ③ 参数 → ④ 运行 → ⑤ 结果。
- 顶部 **开始运行 / 打开输出目录 / 帮助** 按钮可随时调用。
- **帮助 → 帮助中心** 可在线阅读 用户手册/架构/定版记录/打包说明,并可“在外部打开”。

### ① 输入

- **受体(大分子)**:点击“添加受体…”,支持 **PDB(`.pdb`/`.ent`)** 与
  **mmCIF(`.cif`/`.mmcif`)**。可多选、可添加多个受体。选中受体后点 **“链/共晶…”**
  可设置**对接链**(多选)并查看该链检测到的共晶配体。
- **配体库(SDF/MOL/SMILES/CSV)**:一个 SDF 可含多个分子;也可以添加多个文件。
  从 v2.0 起支持 **SMILES**(`.smi`)与 **CSV**(表头需含 `SMILES`,可加 `ID`/`NAME` 列),
  软件用 RDKit 自动生成 3D 构象(ETKDG+MMFF)后走与 SDF 完全相同的准备管线;
  生成失败的分子会逐条记录,不影响其他分子。
- **输出目录**:点击 **“浏览文件夹…”** 用系统对话框选择文件夹,无需手动输入路径。
  所有结果、报告、图件都写入该文件夹。
- **“示例: 4DFR 演示数据”**:一键载入 DHFR+MTX 演示,方便熟悉流程。

### ② 结合位点(搜索盒子)

每个受体都需要一个盒子,有四种方式:

1. **自动(共晶配体质心)** — 最常用。软件检测到共晶配体后以它的重原子质心为中心,
   盒子尺寸为 `max(22 Å, 配体包络 + 12 Å)`(上限 32 Å)。
2. **手动(中心 + 尺寸)** — 你从文献/实验知道位点坐标时使用。单位是 **Å**。
3. **残基中心** — 输入口袋残基,如 `TRP168,ASP27` 或纯编号 `27,31,54`。
4. **盲对接(全蛋白)** — 探索性,假阳性高,一般不建议发表使用。

操作:选中受体 → 选方式 → 填参数 → **“预览盒子”**。若某受体既无共晶配体也没手动
定义,点“开始运行”时会弹窗让你选盲对接或回去定义。软件**绝不静默猜测**口袋。

### ③ 参数

| 名称 | 默认 | 建议 |
| --- | --- | --- |
| 初筛 exhaustiveness | 16 | 想快可降到 8;发表级建议 16–32 |
| 精修 exhaustiveness | 32 | Top-K 前对短名单重对接 |
| 每配体 pose 数 | 9 | 一般不用改 |
| energy_range | 4.0 kcal/mol | 输出多少能量范围内的 pose |
| 精修短名单 | 12 | 每受体取前 12 名精修 |
| 最终 Top-K | 5 | 每受体输出前 K(文件按实际 K 命名) |
| 并行 CPU | 2 | 单个对接用多少线程;越大越快,别超过物理核数 |
| 并行任务数(v2.0) | 1 | >1 时用**进程池**同时对接多个配体(数千配体时显著提速)。每个任务仍用“并行 CPU”线程;=1 为传统串行 |
| 配体目标 pH | 7.4 | 质子化简单规则的目标 pH |

**开关**:回贴验证、精修、**导出对接准确性评估报告**、PLIP 交互、PyMOL 图件、
`.pse` 会话可按需开关。v2.0 新增 **“富集度验证模式”**(见下)。

> **对接准确性评估报告**:勾选后,流水线会在含共晶配体的受体上做“共晶配体
> 回贴(self-docking redocking)”,把对接最低能姿态与晶体姿态比较,输出
> `reports/03_accuracy_assessment.md`、`reports/accuracy_assessment.csv` 与
> `reports/accuracy_poses.csv`。判定:mode-1 RMSD ≤ 1.0 Å 高精度;≤ 2.0 Å 可接受;
> > 2.0 Å FAIL。无共晶配体受体如实标注“不可评估”,不会虚构。从 v2.0 起,RMSD 主口径为
> **对称性感知**(对称等价类内原子互换不计误差),贪心最近邻值保留为对比列;对称性感知
> 不可用时如实退回贪心并标注。

> **富集度验证模式(v2.0,可选)**:在 ③ 参数勾选“富集度验证模式”,并分别选择
> **已知活性文件**与**诱饵文件**(SDF/SMILES/CSV)。这些分子也必须出现在你的配体库中并
> 成功对接。流水线结束后会输出 `results/enrichment/` 下的 ROC 曲线、AUC、EF1%/5%
> 与逐分子判定 CSV,并把结果写进方法与 HTML 总报告。**注意**:这是“验证这把排序可不可信”
> 的实验设计,软件只在你有真实活性/诱饵数据时计算,绝不自动生成活性数据。

### ④ 运行

- **“开始 / 断点续跑”**:首次点开始;运行中断(关闭软件、时间片、出错)后再次点击会
  自动跳过已完成的对子(`done.flag`),从断点继续。
- **“停止(阶段结束后)”**:在当前阶段结束后停止,方便中途调整。
- 日志区实时显示每个受体-配体对的对接进度与清洗提示。

**首次运行耗时**取决于受体大小、配体可旋转键数、exhaustiveness。示例 4DFR+3 配体
在 exhaustiveness 8/10 下约几分钟。

### ⑤ 结果

- 刷新后显示最终 Top-K 表(受体、配体、结合能 kcal/mol、来源)。
- 按钮直达 `reports/`(方法/质量/**准确性评估**报告)与 `results/`(图件、CSV、`.pml`)。
- **“HTML 总报告”**(v2.0):一键打开带目录、Top-K 表、图表、富集度结果与 3D 查看器
  链接的交互式 HTML 报告(在浏览器中查看)。
- **“3D 查看器目录”**(v2.0):每个 Top 复合物一个自包含 HTML 3D 页面(3Dmol.js),
  可旋转/缩放查看受体口袋残基与配体,并附 PLIP 相互作用文本。
- **“打包导出 .zip”**(v2.0):把整个输出目录(结构、对接结果、报告、CSV/JSON/HTML、
  环境 json)打成一个带版本与时间戳的 zip,便于作为可复现附件提交。
- **“打开准确性评估报告”** 直接打开回贴验证的正式评估文档。

## 输出里有什么(举例)

- `docking/4DFR__demo_1/best.pdbqt` —— 该配体最优结合构象
- `results/3D_poses/4DFR__demo_1.png` —— 3D 结合模式图
- `results/pymol_data/4DFR__demo_1/scene.pml` + `complex.pdb` —— 可用 PyMOL 打开编辑
- `results/pse/4DFR__demo_1.pse` —— PyMOL 会话(如勾选)
- `results/final_top{K}.csv`、`reports/01_methods_report.md` —— 论文/答辩材料
- `reports/03_accuracy_assessment.md` —— 对接准确性评估(启用时)
- `reports/html/index.html` + `results/html_viewers/*.html` —— 交互式 HTML 总报告与 3D 查看器(v2.0)
- `results/enrichment/` —— ROC/AUC/EF 富集度验证(启用并给活性/诱饵时,v2.0)
- `run_summary.json` —— 阶段统计与吞吐(v2.0)
- `DockStudio_*.zip` —— 一键打包的可复现数据包(结果页按钮导出)

## 科学严谨性说明

- 所有数值来自真实运行;报告只记录真实执行的步骤,不虚构。
- 无共晶配体受体的对接结果一律标注为**探索性**。
- 质子化采用**文档化简单规则**(羧酸在目标 pH ≥ 5 去质子化;碱性胺在 pH ≤ 8.5
  质子化),不是 pKa 预测工具;手性、互变异构、关键可电离基团需人工复核。
- 受体清洗会移除水/离子/配体;若被移除的离子/辅因子/配体位于搜索盒内或附近,
  会在方法与质量报告中**如实给出清洗提示**。
- 自对接 RMSD 采用**对称性感知匹配**(等价类内原子互换不计误差);贪心最近邻值保留
  为对比列;对称性感知不可用时会如实退回贪心并在 `rmsd_method`/`symmetry_used` 标注。
- 富集度验证只在提供已知活性/诱饵数据时启用;ROC/AUC/EF 全部来自真实对接得分。

## 常见问题

**点开始没反应 / 报“找不到 vina”**:环境没装好。看打包说明或 `python -c "import vina"`。

**盒子没定义**:回到 ② 结合位点给每个受体定义盒子。

**结果里某配体缺失**:查看日志;该配体可能 multi-fragment(多共价片段)被跳过,
或准备失败(报告中列明)。

**回贴验证显示 FAIL(RMSD > 2 Å)**:这是真实结果,不掩盖。可提高 exhaustiveness
重跑 selfdock,或检查质子化/盒子设置。含对称基团(羧酸/磷酸/对称芳香)的配体请先确认
报告中 `symmetry_used` 为 yes;若为 no(对称性感知不可用),FAIL 可能源于匹配未做对称修正。

**如何让软件支持几万个配体不崩?**:在 ③ 参数把“并行任务数”调为 2–8(别超过物理核),
并把“并行 CPU”保持在 1–2;软件按配体分批+断点续跑,可在中途停止后继续。首次请先用
小规模确认参数,再放全量。
