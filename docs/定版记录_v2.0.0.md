# DockStudio 版本定版记录

> 品牌:© 2026 Eric Studio · 版权所有。
> 定版版本:**v2.0.0(桌面级可发表虚拟筛选工作台)** · 定版日期:**2026-09-08**

---

## 0. 交付物

| 交付物 | 位置 / 值 |
| --- | --- |
| 源码版本 | `dockstudio/_version.py` → `2.0.0` |
| Windows 安装包 | `dist/DockStudio_Setup_2.0.0.exe`(4,981,873 字节,见 §6;学术严谨性审查后重建) |
| 安装包 MD5 | `4241cfd78270f8a3d92e38df7889b09c`(见 §6) |
| Linux/macOS 产物 | `packaging/cross/` 脚本 + `packaging/ci/release_all.sh`(见 §6) |

## 1. 变更摘要(相对 v1.1.0)

v2.0.0 按「桌面级可发表虚拟筛选工作台」定位交付,核心圈定为路线图 §6.3 的
#15/#16/#17/#18/#20/#23,并按用户确认把前置补丁 #1(对称性 RMSD)、#2(SMILES/CSV
输入)、#13(多核任务池)并入本次版本。**不引入** Fpocket / gnina / 多打分共识
(保持纯 Python/pip,安装包体积与科学边界可控)。

### 1.1 科学严谨性

1. **对称性感知 RMSD(#1)**:自对接与准确性报告的主口径从“同元素贪心最近邻”升级为
   “对称等价类感知”的 RMSD。等价类由参考分子拓扑图的 **Weisfeiler-Lehman 颜色细化**
   计算(忽略键级/电荷,使羧酸/磷酸等共振等价氧归为同类),同类内原子用匈牙利算法
   重排使 RMSD 最小;**贪心最近邻值保留为对比列**(`*_greedy*`)。对称性不可用(分子
   过大)时如实退回贪心并在 `rmsd_method`/`symmetry_used` 标注。涉及 `core/symrmsd.py`、
   `selfdock.py`、`accuracy.py` 与报告文本。单测含对称合成案例。
2. **ROC/AUC 富集度验证工作流(#16)**:`core/enrich.py`。仅当用户在 GUI ③启用
   “富集度验证模式”并给出**已知活性 + 诱饵**文件(SDF/SMILES/CSV)时运行;标签按
   **中性化规范 isomeric SMILES** 匹配(pH 质子化形式与中性标签视为同一分子),用
   **初筛(single-stage)mode-1 Vina 亲和力**计算 AUC(Mann-Whitney,平均秩处理并列)、
   EF1%/5%,输出 ROC 曲线(SVG)、ROC 点 CSV、逐分子 CSV、`enrichment_summary.json/.csv`。
   缺失数据/文件时如实跳过,**绝不自动生成活性或诱饵**。
3. **方法学披露升级**:报告/README/用户手册同步说明以上口径;新增的 v2.0 阶段均在
   报告留痕(对称性方法、并行参数、富集度方法学与数据来源)。

### 1.2 高通量与交互

4. **VS 规模吞吐 + 多核任务池(#13/#15)**:`core/batch.py` 增加 **ProcessPool 并行
   对接**(`n_workers>1`,每 worker 用 `cfg.cpu` 线程,保证结果与串行同参数一致)、
   **流式读取结果**(`iter_results`,避免 10 万级 result.json 全载内存)、按配体断点
   续跑、分批有界在飞任务;`run_summary.json` 记录各阶段真实 总数/成功/失败/耗时/吞吐。
5. **SMILES/CSV 配体批量输入(#2)**:`core/ligand.py`/`inventory.py` 支持 `.smi`/
   `.csv`(ETKDG+MMFF 生成 3D、逐分子失败记录、SMILES→来源留痕);GUI ①可添加。
6. **交互式 HTML 3D 查看器 + HTML 总报告(#17)**:`core/vizhtml.py`。每个 Top 复合物
   导出 `results/html_viewers/*.html`(3Dmol.js;口袋残基自动圈定 + PLIP 相互作用文本;
   不绘制几何猜想的“相互作用线”,如实披露);`reports/html/index.html` 为 md 总报告
   的姊妹件(目录、环境、Top-K、准确性、富集度、查看器链接)。内置 3Dmol.js 时完全
   离线;未内置时回退 CDN 并在页面注明。
7. **数据打包导出(#18)**:`core/project.py`。一键把整个输出目录打成带版本+时间戳的
   `.zip`(含源文件/结果/报告/CSV/JSON/HTML/environment.json);另有 `.dsproj`
   工程封装(向前兼容读取旧 `config.json`),headless CLI 可读取。
8. **headless CLI + 跨平台(#20)**:`python -m dockstudio run <config.json|.dsproj>`
   [--out …][--phases …][--time …];GUI-first 定位不变。新增 `packaging/cross/`
   (Linux/macOS conda-pack 绿色目录脚本 + 文档)与 `packaging/ci/release_all.sh`
   (三平台产物 + MD5 + Release 说明编排)。
9. **CITATION.cff(#23)**:仓库根新增机器可读引文元数据;README 增加“如何在论文引用
   DockStudio”;DOI 注册动作由作者在 Zenodo 完成(操作步骤写进 README/CITATION.cff)。

### 1.3 兼容性

- `RunConfig` 新增字段全部**默认关闭或纯增量**:`n_workers=1`、`run_enrichment=False`、
  `actives_path/decoys_path=""`、`use_symmetry_rmsd=True`、`run_html_report=True`。
- 既有 `config.json` 可被原样读取;旧 `final_top{K}.csv`、`docking/`、`refine/`、
  `reports/` 等文件名与目录结构不变(仅新增列/新增目录)。
- `selfdock_summary.csv`/`accuracy_assessment.csv` 新增贪心对比列与对称性列(列追加,
  旧列名保留在表头靠前位置)。

## 2. 仍保留的已知说明(如实)

- 质子化仍是文档化简单规则(非 pKa 预测);手性/互变异构/关键可电离基团需人工复核。
- 无共晶配体受体结果标注探索性、回贴验证标注“不可评估”。
- 对称等价类用颜色细化近似自同构轨道;对高度规则的对称图(罕见)可能把本不等价的
  原子并入同类,影响仅是把 RMSD 算得略宽松,报告披露该方法。
- ROC/AUC 是“这把 Vina 排序在该体系下好不好”的验证指标,不等于活性预测精度;
  若部分活性/诱饵未对接成功,报告给出标签流失计数并如实披露。
- HTML 查看器中的口袋残基由几何距离(6 Å)圈定,仅供交互查看;不构成实验结论。
- HTML 3D 查看器在本仓库未内置 `3Dmol-min.js` 本地副本,生成页面回退 CDN(需联网);如需完全
  离线,把 `3Dmol-min.js` 放到 `dockstudio/resources/` 后重新生成(页面会如实标注)。
- Windows 安装包已重建并给出 MD5(见 §6);**GUI 未在真实 Windows 实跑**(沙箱无 tkinter),
  需用户在本机双击安装并回归(见交付摘要建议清单)。

## 3. 涉及模块清单

新增:`core/symrmsd.py`、`core/enrich.py`、`core/vizhtml.py`、`core/project.py`;
修改:`core/models.py`、`core/selfdock.py`、`core/accuracy.py`、`core/batch.py`、
`core/ligand.py`、`core/inventory.py`、`core/cofactor.py`、`core/qc.py`、
`core/pipeline.py`、`core/reports.py`、`gui/app.py`、`__main__.py`、`_version.py`、
`README.md`、`用户手册.md`、`packaging/打包说明.md`、新增 `packaging/cross/*`、
`packaging/ci/*`、`CITATION.cff`、`examples/demo/ligand_library.sdf`(修复 CCD 键级后重生成)、
`tests/test_symrmsd.py`、`tests/test_enrich.py`、`tests/test_smiles_input.py`、
`tests/test_project.py`、`tests/test_vizhtml.py`、`tests/test_ligand_ph.py`、
`tests/test_cofactor.py`、`tests/run_smoke.py`(legacy/new 开关)。
学术严谨性审查记录:`docs/学术严谨性审查记录_v2.0.0.md`(详见该文档 §1 缺陷清单)。

## 4. 测试记录(2026-09-08 Linux 沙箱实测)

沙箱环境:Linux + Python 3.10;rdkit 2026.03.6、meeko 0.8.0、vina 1.2.7(python 后端)、
gemmi 0.7.5、prody 2.6.1、plip 3.0.1。GUI 无 tkinter,仅语法编译 + 代码审查。

1. `python -m compileall -q dockstudio tests` → **通过**。
2. `python -m pytest tests -q` → **44 passed**(新增 7 个测试文件:
   `test_symrmsd.py`/`test_enrich.py`/`test_smiles_input.py`/`test_project.py`/
   `test_vizhtml.py`/`test_ligand_ph.py`/`test_cofactor.py`)。
3. 无头端到端 smoke(学术严谨性审查后重跑;`examples/demo` 已用修复后的 CCD 键级解析重建):
   - **旧配置** `python tests/run_smoke.py 140 --out /tmp/smoke_audit_legacy --legacy` → 完成,
     `interrupted=false`,html/enrich 关闭,旧文件结构不变。
   - **新配置** `python tests/run_smoke.py 140 --out /tmp/smoke_audit_new --workers 2` → 完成,
     `interrupted=false`;final_top3: demo_1(MTX)**-8.77**、demo_3 -7.49、demo_2 -6.74 kcal/mol;
     `reports/html/index.html` + 3 个 `results/html_viewers/*.html` 生成。
   - selfdock(MTX 回贴,exh=10)新配置 mode1 对称 RMSD **5.423 Å** vs 贪心对比列 **5.459 Å**
     (低 exh 的预期 FAIL,如实报告;`symmetry_used=yes`)。
4. headless CLI `python -m dockstudio run /tmp/smoke_audit_new/config.json --phases html`
   → **exit 0**。
5. Windows 安装包重建 + MD5:见 §6。

> **第一轮(编码验证)修复**:`__main__.py` f-string 语法;`symrmsd.py` 单一对称轨道不触发
> 匈牙利重排;`vizhtml.py` HTML 查看器未注入 3Dmol.js loader。
>
> **第二轮(学术严谨性审查)修复(A1-A6,详见 `docs/学术严谨性审查记录_v2.0.0.md`)**:
> A1 羧基 pH 去质子化静默失效;A2 CCD 键级大写码被当小写、全键变单键;A3 富集度标签
> 匹配未中性化且 SMILES 源未规范化(可电离分子必流失);A4 富集度得分混用 refine 与
> screening(会虚高);A5 QC「精修 done.flag」恒真;A6 盘点与准备配体命名不一致。

## 5. 验证清单(供执行者/用户)

1. `python -m compileall -q dockstudio tests` 通过。
2. `python -m pytest tests -q` 通过(含新增 7 个测试文件)。
3. 无头 smoke 两种配置:
   - 旧配置:`python tests/run_smoke.py 150 --out <tmp> --legacy`
   - 新配置:`python tests/run_smoke.py 150 --out <tmp2>`(symmetry/html on)
4. `python -m dockstudio run examples/demo_out/config.json --out <tmp3>`(headless CLI)
5. 重建 Windows 安装包 + MD5;若用户需要,产出 Linux 产物。

## 6. 构建 / 发布状态(2026-09-08 实测)

- **Windows 安装包已重建(学术严谨性审查后)**:`dist/DockStudio_Setup_2.0.0.exe`,
  4,981,873 字节;**MD5 `4241cfd78270f8a3d92e38df7889b09c`**(Linux 沙箱 + conda-forge
  NSIS 编译;payload 内置 micromamba.exe / vina_1.2.7_win.exe / VC++ 运行库与含审查修复的
  v2.0.0 源码)。
- Linux/macOS 绿色目录产物脚本与文档:`packaging/cross/`(README_cross.md +
  build_condapack_unix.sh);三平台产物 + MD5 + Release 说明编排:`packaging/ci/release_all.sh`。
- GitHub 发布链路(由作者/后续会话执行):`git add -A && git commit … && git push origin main
  && git tag v2.0.0 && git push origin v2.0.0`,然后
  `gh release create v2.0.0 dist/DockStudio_Setup_2.0.0.exe …`(参考 v1.1:
  https://github.com/Eric-lab-cpu/DockStudio )。

## 7. 后续迭代约定

自本记录起,以 **v2.0.0 为基线**;后续 v2.1/v3 按用户要求递增版本号,并在本节追加变更。
